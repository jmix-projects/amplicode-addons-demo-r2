# amplicode-addons-demo-r2

## Installation

### Backend
Create an empty PostgreSQL database with a name: `amplicodeaddonsdemo`.

Start application.

### Frontend

Go to `/frontend` directory.

Run the following commands:
```shell
npm install
```
```shell
npm run start
```